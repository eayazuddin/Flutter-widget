import 'package:flutter/material.dart';
class Contactfragement extends StatelessWidget {
  const Contactfragement({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Contactfragement"),
      ),
    );
  }
}