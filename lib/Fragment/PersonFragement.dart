import 'package:flutter/material.dart';
class Personfragement extends StatelessWidget {
  const Personfragement({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Personfragement"),
      ),
    );
  }
}