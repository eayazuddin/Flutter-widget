import 'package:flutter/material.dart';
class Settingsfragement extends StatelessWidget {
  const Settingsfragement({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Settingsfragement"),
      ),
    );
  }
}