import 'package:flutter/material.dart';
class Emailfragement extends StatelessWidget {
  const Emailfragement({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Emailfragement"),
      ),
    );
  }
}