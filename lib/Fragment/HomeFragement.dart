import 'package:flutter/material.dart';
class Homefragement extends StatelessWidget {
  const Homefragement({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Homefragement"),
      ),
    );
  }
}